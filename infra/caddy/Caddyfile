# DropDeploy edge — TLS termination only.
#
# This file terminates TLS and hands every request to the Next.js app with the
# original Host header intact. It deliberately does NOT route to containers:
# `src/proxy.ts` is the single authoritative router, and it is where the
# private-URL gate and the ProxyHit analytics live. An edge that proxied a
# tenant hostname straight to a container port would silently bypass both.
#
# Placeholders to substitute before use:
#   {$BASE_DOMAIN}          e.g. app.en3.wtf
#   {$APP_UPSTREAM}         e.g. 127.0.0.1:3000   ← check the real port
#   {$INTERNAL_EDGE_TOKEN}  the shared secret from the app's env
#   {$ACME_EMAIL}           contact address for Let's Encrypt
#   {$WILDCARD_CERT} / {$WILDCARD_KEY}  paths to the existing certbot wildcard

{
	# Contact address for Let's Encrypt — where issuance and expiry warnings go.
	#
	# NOTE: `email` with an empty value is a Caddyfile PARSE ERROR, not a no-op,
	# so ACME_EMAIL must always be set. docker-compose.caddy.yml enforces that
	# with `${ACME_EMAIL:?...}`, which fails at `up` with a readable message
	# rather than letting Caddy refuse to start on a config it cannot parse.
	email {$ACME_EMAIL}

	# ── On-demand TLS ─────────────────────────────────────────────────────
	# Caddy calls `ask` during the TLS handshake for any hostname it has no
	# certificate for. A 200 authorises an ACME order; anything else fails
	# the handshake with no order placed.
	#
	# The token travels as a query parameter because Caddy's `ask` is a plain
	# GET with no header support; Caddy appends `domain=<host>` to whatever
	# query string is already there. Keep this URL on loopback so the token
	# never crosses a network.
	#
	# Caddy 2.10 REMOVED the `interval`/`burst` options this block used to
	# carry (verified against 2.11.4). There is therefore no rate limit at the
	# edge any more: the ask endpoint is now the ONLY throttle standing between
	# a hostname scan and the Let's Encrypt quota of 50 certificates per
	# registered domain per week. That makes `checkTlsAskRateLimit` in
	# src/lib/rate-limit.ts load-bearing rather than defence in depth — do not
	# relax or remove it.
	on_demand_tls {
		ask http://{$APP_UPSTREAM}/api/internal/tls-check?token={$INTERNAL_EDGE_TOKEN}
	}

	# Uncomment for the first production run. Staging certificates are not
	# trusted by browsers, but its rate limits are far higher — validate the
	# whole flow here before spending real quota.
	# acme_ca https://acme-staging-v02.api.letsencrypt.org/directory
}

# ── Reusable upstream block ───────────────────────────────────────────────────
(app_upstream) {
	# The ask endpoint must be reachable by the edge and nobody else. Caddy
	# calls it directly on {$APP_UPSTREAM}, which does not pass through this
	# site block, so refusing it here costs nothing and removes the endpoint
	# from the public internet entirely. Its token check still stands behind
	# this — defence in depth, not instead of.
	@internal path /api/internal/*
	respond @internal 403

	reverse_proxy {$APP_UPSTREAM} {
		# The router resolves the project from the Host header, so it must
		# arrive unmodified.
		header_up Host {host}
		header_up X-Real-IP {remote_host}
		header_up X-Forwarded-Proto {scheme}

		# Deployed apps stream logs (SSE on /logs/stream) and may hold
		# long-lived connections. Without this, Caddy buffers and the stream
		# arrives in chunks or not at all.
		flush_interval -1
	}

	encode zstd gzip

	# Baseline hardening for everything the platform serves. Note there is no
	# CSP here: the content is arbitrary tenant applications, and a platform-wide
	# CSP would break them. Tenants set their own.
	header {
		Strict-Transport-Security "max-age=31536000; includeSubDomains"
		X-Content-Type-Options "nosniff"
		Referrer-Policy "strict-origin-when-cross-origin"
		-Server
	}
}

# ── Platform + tenant subdomains ──────────────────────────────────────────────
# Served from the EXISTING certbot wildcard, not re-issued by Caddy.
#
# This is deliberate for the nginx→Caddy cutover: reusing the cert files on disk
# means the swap places zero ACME orders, so the platform cannot go dark because
# an order failed or hit a rate limit. certbot keeps owning this certificate.
#
# It is not the end state. That certificate is `authenticator = manual` and so
# does NOT auto-renew — see docs/nginx-to-caddy-cutover.md, which covers moving
# wildcard issuance into Caddy over DNS-01 before it expires.
*.{$BASE_DOMAIN}, {$BASE_DOMAIN} {
	tls {$WILDCARD_CERT} {$WILDCARD_KEY}
	import app_upstream
}

# ── Follow-up: let Caddy own the wildcard via DNS-01 ──────────────────────────
# Replace the block above with this once a Route 53 credential exists and the
# image has been rebuilt with the plugin (xcaddy --with github.com/caddy-dns/route53).
# This is what makes wildcard renewal automatic.
#
# *.{$BASE_DOMAIN}, {$BASE_DOMAIN} {
# 	tls {
# 		dns route53 {
# 			max_retries 10
# 		}
# 	}
# 	import app_upstream
# }

# ── Custom domains ────────────────────────────────────────────────────────────
# Catch-all for every other hostname. `on_demand` means no certificate is held
# in advance; one is ordered at handshake time, and only if the ask endpoint
# authorises it. Renewal is automatic — there is no cron to maintain.
#
# These use HTTP-01/TLS-ALPN, which need no DNS credentials, which is why custom
# domains work from day one even while the wildcard is still certbot's.
:443 {
	tls {
		on_demand
	}
	import app_upstream
}

# Port 80 exists for the HTTP-01 challenge and to redirect humans. Caddy answers
# /.well-known/acme-challenge/* itself; `src/proxy.ts` leaves that path alone.
:80 {
	redir https://{host}{uri} permanent
}
